#include "monty.h"

/**
 * no_operation - doesn't do anything at all.
 * @top: top of the stack
 * @line_number: number of current code line in editor
 *
 * Return: void
 */
void no_operation(stack_t **top, unsigned int line_number)
{
	(void) top;
	(void) line_number;
}
